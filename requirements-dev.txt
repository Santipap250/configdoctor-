# OBIXConfig Doctor — Development Requirements
# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
# Development-only dependencies (NOT for production)
# Usage: pip install -r requirements.txt -r requirements-dev.txt

# Include all production requirements
-r requirements.txt

# ─── Testing ───────────────────────────────────────────────────
pytest==7.4.0
pytest-cov==4.1.0
pytest-flask==1.2.0
pytest-mock==3.11.1

# ─── Code Quality ──────────────────────────────────────────────
flake8==6.0.0
pylint==2.17.5
black==23.7.0
isort==5.12.0

# ─── Development Tools ─────────────────────────────────────────
ipython==8.14.0
ipdb==0.13.13
