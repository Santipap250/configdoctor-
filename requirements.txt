# OBIXConfig Doctor — production requirements
# ─────────────────────────────────────────────────
# FIX L2: Flask 2.3.x ต้องการ Werkzeug 2.x เท่านั้น (incompatible กับ 3.x)
#          แก้โดย upgrade Flask → 3.0.x ซึ่ง fully compatible กับ Werkzeug 3.0.x
# SECURITY PATCH:
#   [HIGH]   gunicorn >= 22.0.0  (CVE-2024-1135: HTTP Request Smuggling)
#   [HIGH]   Werkzeug >= 3.0.3   (CVE-2023-46136: multipart DoS)
#   [MEDIUM] Jinja2 >= 3.1.4     (CVE-2024-34064: XSS via xmlattr filter)
# FIX v5.2: Flask-WTF สำหรับ CSRF Protection ทุก POST route
# FIX v5.2: Flask-Compress ลด HTML/JSON size ~70%
# FIX v5.1: Flask-Limiter ป้องกัน API abuse/spam
Flask==3.0.3
gunicorn==22.0.0
Werkzeug==3.0.3
python-dotenv==1.0.0
Jinja2>=3.1.4,<4.0
Flask-Limiter==3.5.0
Flask-Compress==1.14
Flask-WTF==1.2.1
limits[redis]>=3.6.0
