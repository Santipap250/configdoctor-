# OBIXConfig Doctor v5.2 — Production Requirements
# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
# SECURITY: All versions pinned to tested & secure releases

# ─── Flask Framework ───────────────────────────────────────────
# v3.0.3+: Full Werkzeug 3.x compatibility, security patches
Flask==3.0.3
Werkzeug==3.0.3
Jinja2>=3.1.4,<4.0

# ─── Server & Deployment ───────────────────────────────────────
# gunicorn v22.0.0+: CVE-2024-1135 HTTP Request Smuggling fix
gunicorn==22.0.0
python-dotenv==1.0.0

# ─── Security & Protection ─────────────────────────────────────
# Flask-WTF v1.2.1: CSRF token protection for all POST routes
Flask-WTF==1.2.1

# Flask-Limiter v3.5.0: Rate limiting API abuse prevention
Flask-Limiter==3.5.0
limits[redis]>=3.6.0

# ─── Performance ───────────────────────────────────────────────
# Flask-Compress: gzip/brotli compression (~70% size reduction)
Flask-Compress==1.14

# ─── Data Processing ───────────────────────────────────────────
# CSV/data analysis for Blackbox logs
numpy>=1.24.0
pandas>=2.0.0

# ─── Image Processing ──────────────────────────────────────────
# OSD designer & graphics generation
Pillow>=10.0.0

# ─── Optional: Testing ─────────────────────────────────────────
# Uncomment to include in pip install -r requirements.txt
# pytest==7.4.0
# pytest-cov==4.1.0
# pytest-flask==1.2.0
