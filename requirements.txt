# OBIXConfig Doctor v5.1 — production requirements
# ─────────────────────────────────────────────────
# FIX: เพิ่ม Flask-Limiter เพื่อป้องกัน API abuse/spam
# FIX: Pin Jinja2 ไม่ให้ auto-upgrade เป็น v4 ที่อาจ breaking
Flask==2.3.3
gunicorn==21.2.0
Werkzeug==2.3.7
python-dotenv==1.0.0
Jinja2>=3.1.2,<4.0
Flask-Limiter==3.5.0
